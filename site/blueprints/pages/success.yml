title: Success Page

columns:
  main:
    width: 2/3
    sections:
      fields:
        type: fields
        fields:
            main_image:
              label: Main Image
              type: files
            page_content:
              label: text
              type: textarea
  sidebar:
    width: 1/3
    sections:
      pages:
        type: pages
        template: default
      files:
        type: files

